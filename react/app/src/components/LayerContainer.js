import { Container } from '@mui/material';

const LayerContainer = (props) => {

    return(
        <Container>
            <div>{props.grid}</div>
        </Container>
    );
}

export default LayerContainer;
